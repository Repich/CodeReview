# План дальнейших работ

1. **Авторизация и личный кабинет**
   - Дальше — переключиться с локальной email+пароль схемы на полноценный OIDC/JWT-провайдер.
   - Расширить роли (user/admin уже есть) тарифами, подарками, промокодами и управлением пользователями.
   - UI: форма пополнения/покупки баллов самим пользователем, отображение детальных транзакций.

2. **Очередь и worker**
   - Включить Redis (или другую очередь) вместо `GET /next-task` polling.
   - Добавить логирование trace и повторные попытки (retry/backoff).
   - Собирать метрики по детекторам, длительность, статус.

3. **Детекторы и аналитика**
   - Расширить `norms.yaml` и добавить новые детекторы (AST/LLM-этап).
   - Поддерживать документированный маппинг норм → источник (`Система стандартов...`, `Руководство разработчика`) и обновлять раздел «Нормы и стандарты» в `docs/architecture_overview.md` при изменениях.
   - Собирать обратную связь (Feedback) в UI, строить отчёты TP/FP/FN.
   - Выгрузка findings в JSONL уже готова — интегрировать с downstream системами.

4. **Backend API**
   - Endpoints для IOLog/артефактов: список, скачивание, удаление.
   - Экспорт findings в архивы, генерация ссылок для скачивания.
   - Авторизация на всех endpoints (токены, rate limiting).

5. **UI улучшения**
   - Поле загрузки файлов/архивов для run'а, выбор норм, отображение AuditLog/Feedback в реальном времени.
   - Notification/Toasts, отображение баланса и прогресса прямо в форме.

6. **DevOps**
   - Автоматизировать миграции Alembic, обновить docker-compose, добавить в CI прогон backend тестов/линтеров.
   - Документация OpenAPI/Swagger, scripts для развёртывания.
